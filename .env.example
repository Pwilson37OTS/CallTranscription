# OpenAI API key (required)
OPENAI_API_KEY=your-api-key-here

# Optional: override default models
# TRANSCRIPTION_MODEL=gpt-4o-transcribe
# SUMMARY_MODEL=gpt-4.1

# Optional: max upload size in MB (default: 500)
# MAX_UPLOAD_SIZE_MB=500

# Admin account (created on first run)
ADMIN_EMAIL=admin@oaktreestaffing.com
ADMIN_DEFAULT_PASSWORD=changeme123

# Optional: max API calls per user per hour (default: 50)
# RATE_LIMIT_PER_HOUR=50

# --- CloudCall Integration (optional) ---
# Set CLOUDCALL_API_KEY to enable the integration. Leave blank to disable.
# CLOUDCALL_API_BASE_URL=https://api.cloudcall.com/v1
# CLOUDCALL_API_KEY=your-cloudcall-api-key
# CLOUDCALL_API_SECRET=your-cloudcall-api-secret
# CLOUDCALL_WEBHOOK_SECRET=your-webhook-signing-secret
# CLOUDCALL_RECORDING_RETENTION_HOURS=48
